{
    "name": "lens-backend",
    "main": "src/index.ts",
    "private": true,
    "engines": {
        "node": "20 || 22 || 24"
    },
    "scripts": {
        "dev": "genkit start -- npx nodemon --ext prompt --watch prompts --exec \"tsx --watch src/index.ts\"",
        "dev:next": "next dev",
        "build": "tsc",
        "start": "node dist/index.js",
        "lint": "eslint src --ext .ts"
    },
    "dependencies": {
        "@genkit-ai/firebase": "^1.12.0",
        "@genkit-ai/vertexai": "^1.12.0",
        "genkit": "^1.12.0"
    },
    "devDependencies": {
        "@types/node": "^20",
        "eslint": "^8",
        "genkit-cli": "^1.12.0",
        "nodemon": "^3.0.0",
        "prettier": "^3.3.3",
        "tsx": "^4.0.0",
        "typescript": "^5.5.3"
    }
}